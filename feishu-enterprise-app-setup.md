# 飞书企业自建应用配置

## 目标链路

```text
飞书企业自建应用
  -> WebSocket 长连接
  -> 飞书连接器（Feishu Connector）
  -> 本机 cc-connect 桥接服务（Bridge）
```

本项目使用带机器人能力的企业自建应用。群自定义 Webhook 机器人只能单向通知，不能替代这里的应用机器人。

## 1. 创建并发布应用

在飞书开放平台开发者后台完成：

1. 在目标租户中创建企业自建应用。
2. 添加机器人能力。
3. 在事件订阅中选择“使用长连接接收事件”。
4. 订阅 `im.message.receive_v1`。
5. 仅添加部署确实需要的消息接收与发送权限。
6. 将应用可用范围设置为包含主人和必要测试用户。
7. 创建并发布应用版本后再测试；未发布版本通常不会应用新增权限。

后台中文名称可能变化，应按权限用途选择，不要复制一整套宽权限。

## 2. 最小权限

基础部署需要以下用途对应的权限（后台可能显示本地化名称）：

```text
im:message.p2p_msg:readonly
im:message.group_at_msg:readonly
im:message:send_as_bot
```

它们分别用于：

- 接收发给应用机器人的私聊消息；
- 接收群中明确 `@` 应用机器人的消息；
- 以应用机器人身份回复消息。

若需要接收其他机器人转交的任务，再额外申请：

```text
im:message.group_at_msg.include_bot:readonly
```

该权限允许接收“其他机器人在群中 `@` 当前机器人”的消息。不要仅为查历史发送者申请 `im:message.group_msg`；它会暴露整个群的消息，权限明显更宽。

只有真正启用交互审批卡片时才增加卡片回调权限。当前文字审批流程不需要该权限。

## 3. 安全取得私有身份值

以下值只保存在本机密码管理器、Shell 环境或系统密钥环中，不得发给 Agent、聊天、Issue 或代码仓库。

- **App ID / App Secret**：从飞书开放平台应用凭证页取得；App Secret 只提供给本机进程。
- **主人 `open_id`**：在私密测试会话中，从经过脱敏的 `im.message.receive_v1` 新事件读取 `sender.sender_id.open_id`，或使用租户已批准的通讯录查询；不要为了查 ID 开通群历史全量读取。
- **来源机器人 `open_id`**：发布 `im:message.group_at_msg.include_bot:readonly` 后，让来源机器人在白名单测试群中 `@` 当前应用一次，再从这个新事件读取 `sender.sender_id.open_id`。
- **群 `chat_id`**：从同一个白名单测试事件读取 `message.chat_id`，或使用最小群读取权限从群详情 API 获取。

飞书连接器（Connector）启动时会查询当前应用机器人的自身身份，因此自身 `open_id` 不需要写进配置。共享测试信息前必须先脱敏事件正文。

## 4. 必须保持私密的值

主人需要在本地配置流程中提供：

- App ID；
- App Secret；
- 主人用户 `open_id`；
- 可选的来源机器人 `open_id` 白名单；
- 可选的群 `chat_id` 白名单。

不得把这些值粘贴到公开 Issue、文档、截图或任务消息中。

## 5. 本地配置

通过环境变量准备上述值，再运行 `./scripts/init-config.sh`。脚本会写入：

```text
runtime/feishu-connector/config.json
runtime/cc-connect/config.toml
```

两个文件必须使用 `600` 权限，父目录使用 `700`。生成器会创建彼此独立的 Bridge 令牌和管理接口令牌。桥接服务（Bridge）必须保持在 `127.0.0.1`。

## 6. 验收矩阵

发布应用并启动本地服务后，逐项测试：

| 测试 | 预期结果 |
|---|---|
| 主人发送私聊消息 | 接收并回复 |
| 非白名单用户发送消息 | 忽略 |
| 主人在群中发送未 `@` 消息 | 忽略 |
| 主人在获准群中 `@` 应用机器人 | 接收并回复 |
| 白名单来源机器人在白名单群中 `@` 应用机器人 | 接收并处理 |
| 非白名单来源机器人 `@` 应用机器人 | 忽略 |
| 应用机器人回复自身输出 | 忽略，不形成循环 |
| 卡片或富文本包含链接 | 提取文字和 URL |
| 只有图片、文件或语音的消息 | 当前版本不转发 |

测试使用虚构内容。若截图或日志意外泄露凭据，应立即轮换。

## 7. 常见错误

- 配成群自定义 Webhook，而不是企业自建应用。
- 增加 `include_bot` 权限后忘记发布新版本。
- 开通权限但没有把来源群加入 `allowedBotChatIds`。
- 把 `allowedBotIds` 设为 `*`，或保留过宽群白名单。
- 已有 WebSocket 长连接时仍开放公网 HTTP 回调。
- 把桥接服务（Bridge）或管理接口（Management）端口暴露到本机回环地址（loopback）之外。
