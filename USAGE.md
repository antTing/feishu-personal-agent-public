# 使用指南

## 消息入口

- 私聊：白名单用户的非空支持消息直接进入 Agent。
- 群聊：只处理白名单用户或获准机器人明确 `@` 当前机器人的消息。
- 机器人转交：来源机器人和群都必须在白名单，且应用已取得接收机器人 `@` 消息的权限。
- 支持文本、富文本、卡片中的文字和链接。
- 图片、文件和语音本体当前不会下载或转给 Codex。

飞书连接器不做“免费本地问答”。能力、费用、帮助和开发任务都会通过 cc-connect 进入 Codex；只有任务列表、停止和审批指令由飞书连接器本地处理。

## 任务状态

发送以下任一消息查看当前群或私聊中的活动任务：

```text
任务状态
任务列表
/status
```

每个任务有一个短编号，例如 `T-XXXXXXXX`，并显示项目、阶段和耗时。阶段包括思考、执行、排队、等待审批和停止中。

## 停止任务

只有一个活动任务时：

```text
停止任务
/stop
/cancel
```

有多个活动任务时必须指定编号：

```text
停止任务 T-XXXXXXXX
```

只有配置中的主人用户可以停止任务。停止正在执行的任务会停止其 cc-connect 会话；同一会话中尚未执行的排队任务也可能一起取消。当前版本不支持从 cc-connect 内部队列中只删除中间一条任务。

## 并发与排队

同一发送者、同一工作区对应同一个会话，该会话中的任务顺序执行。不同发送者或不同工作区会话可以并行，但仍受本机 CPU、内存、模型速率和 cc-connect 限制。

需要精确停止时先发送 `任务状态`，按任务编号操作，不要只描述任务标题。

## 工作区路由

没有明确项目的普通问题进入 `agent-workspace/`。消息中明确给出已登记项目名时，飞书连接器才会路由到对应工作区。

- 只读分析进入只读项目（read project）。
- 修改、构建、测试、安装依赖或生成产物进入开发项目（dev project）。
- 未登记项目返回 `WR-...` 工作区申请，批准前不会调用 Codex。
- 工作区查找只发生在配置的安全根目录内；同名结果不唯一时暂停，不猜测路径。
- 找不到项目来源时暂停并请求准确路径或单独的创建/克隆授权。

工作区可以是 Git 仓库，也可以是普通目录。根目录存在有效 `.git` 时，开发任务会返回 `BR-...` 并要求主人确认目标分支；普通目录不设置分支门禁。如果普通目录内后续发现要操作嵌套 Git 仓库，Agent 应报告相对路径和分支并暂停确认。

分支确认示例：

```text
确认分支 BR-XXXXXXXX feature/example
```

确认分支不等于批准任何 Git 命令。切换、创建、提交、合并、拉取、推送等操作仍需要对完整具体命令单独批准。

## 操作审批

开发项目使用 Codex 应用服务（App Server）。写文件、执行命令和其他受控工具操作会返回一次性 `PA-...`：

```text
允许操作 PA-XXXXXXXX
拒绝操作 PA-XXXXXXXX
```

审批只接受配置中的主人用户，并绑定原任务会话。批准只对该次工具请求有效，不授予永久权限，也不自动批准后续 Git、部署、删除、外发或生产操作。

## 回复与会话

状态和最终答复都引用发起任务的原消息；群聊还会 `@` 原发送者。其他机器人转交时，回复会 `@` 该来源机器人。

公开默认配置把 `reset_on_idle_mins` 设为 `0`，不会仅因旧会话空闲而自动切换。如果安装者主动配置为大于 `0`，提示中的“空闲超过 N 分钟”指该会话上一次真实用户消息距当前消息的时间，不是刚发送任务的执行耗时。

飞书任务由 cc-connect 启动独立的 Codex 命令行工具（CLI）或应用服务（App Server）会话，通常不会自动出现在当前 Codex 应用的对话列表中。实时进度以飞书中的任务状态为准。

## 费用

- 飞书企业自建应用使用租户的消息和 OpenAPI 配额，不等于调用飞书智能体模型。
- 如果另一个智能体先生成内容再转交，它可能消耗自己的套餐或模型额度。
- 飞书连接器与 cc-connect 是本地开源组件，没有按消息软件费。
- Codex 的模型消费取决于所选登录方式、账户和当时的产品条款。
- 本机计算、网络、电力，以及后续接入的数据库、云服务或第三方 API，由相应账号承担。

不要根据机器人回答推断账单。实际费用应在对应平台的用量和账单页面核对。
