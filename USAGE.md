# 使用指南

## 推荐使用方式

把 Aily 当作日常入口，把本地机器人当作明确的本机执行器：

- 问候、测试和飞书云端操作由 Aily 直接处理；
- 需要读取本地文件、代码、构建环境或终端时，Aily 在执行群中 `@` 本地机器人；
- 发送图片或普通文件时，原生 cc-connect 负责把媒体交给 Agent；
- 不要把无关聊天历史、密钥、Cookie 或 Token 一起转发。

直接给本地机器人发送普通消息会进入 Codex，可能产生模型用量。项目本身不会自动判断消息是否值得调用 Codex。

## 任务封装

Aily 为独立任务链生成 `DS-XXXXXXXX`，为每条具体任务生成 `T-XXXXXXXX`。示例：

```text
@本地机器人
[LOCAL_TASK]
{"version":1,"chain_id":"DS-1234ABCD","task_id":"T-5678EFGH","workspace":"approved-workspace","permission":"read","branch":null,"request":"检查示例项目的构建配置","context":"只提供本任务需要的背景"}
[/LOCAL_TASK]
```

完整字段和校验规则见 [docs/aily-dispatch-protocol.zh-CN.md](docs/aily-dispatch-protocol.zh-CN.md)。

## 会话、排队和并行

原生配置中的 `thread_isolation = true` 把一个飞书根消息/回复线程作为一个 cc-connect 会话边界。任务排队由 Aily 负责：

> 本仓库不提供 Aily 侧持久队列的运行时实现。以下语义只有在上游通过工作流、数据库或自定义服务真正持久化状态后才成立，不能只依赖模型上下文或系统提示词。

- 同一 `DS` 同时只向 cc-connect 派发一个队首 `T`；后续 `T` 留在 Aily 的持久 FIFO 中；
- 当前运行 `T` 的补充信息可以发到同一线程；新的独立任务不能作为补充信息提前派发；
- 当前 `T` 进入终态后，Aily 才派发下一条；
- 不同任务链应创建不同根消息线程，可以并行；
- 同一个群里 A 和 B 的两段对话，即使中间夹着其他消息，也不是同一任务链；
- 不同群默认相互隔离，即使使用同一个 Aily；
- Aily 应维护 `DS/T -> 来源群、来源根消息、执行线程、状态` 的映射。

不要用“同一个机器人”或“同一个群”单独判断是否续接任务。

## 状态和停止

原生 cc-connect 会在线程内发送处理中、工具进度和完成反馈；固定版本补丁会引用原任务消息并 `@` 原发送者。Aily 收到这些事件后仍应按任务封装中的 `DS/T` 更新持久状态，并把终态转回原始来源。执行群保留的任务控制指令：

```text
/stop
```

原生 `/status` 和 `/usage` 会暴露工作目录、会话或账号信息，因此本项目默认禁用，不把它们作为共享执行群的状态接口。

停止排队中的 `T` 时，Aily 直接从自己的队列移除并标记 `cancelled`，不向 cc-connect 发消息。停止正在运行的 `T` 时，Aily 才在对应线程发送 `/stop`；去掉机器人 `@` 后正文必须只有该命令，不能改写成自然语言。详细线格式见 [Aily 路由规则模板](config/aily-router.example.md)。同一 `DS` 没有其他已派发 `T`，因此不会误停或误清别的任务。`/cancel` 会重置整条 cc-connect 会话，本项目默认禁用，不用于单任务停止。

## 工作区和权限

原生第一阶段配置一个明确工作区，允许它是 Git 仓库，也允许是普通目录：

- 只读任务使用 Codex `suggest` 模式；
- 写文件、命令、部署、删除和外发操作由 cc-connect 的权限提示处理；
- Git 仓库的分支确认应在任务开始前由 Aily/主人完成；
- 普通目录不强制要求 Git；发现嵌套仓库时先报告相对路径和分支；
- 未知工作区不应直接通过消息传入路径，先由主人确认路径和访问范围。

本项目对固定的 cc-connect v1.4.1 增加了窄补丁：项目级和飞书平台级 `approval_from` 都只配置主人。非主人不能通过文字或卡片批准、拒绝；启用该策略时“允许全部”也会从审批卡片中移除并在核心层拒绝，避免审批延续到同线程的后续任务。Aily 仍可回答普通澄清问题和在正确线程停止任务。

原生配置在执行群只开放 `/stop`、`/help`、`/version`。文字命令和卡片按钮共用同一份禁用策略；主人和 Aily 都不能通过飞书查看敏感状态、重置会话、切换模型、Provider、执行模式、工作区或历史会话。需要调整这些配置时，应停止服务后由主人修改私有配置并重新验证，不能在任务消息中临时放宽。

`workspace` 和 `permission` 字段目前是 Aily 的路由元数据，不会让原生 cc-connect 自动切换目录或权限。第一阶段只派发到配置中的固定工作区；其他工作区必须暂停，等主人另行登记和配置后再执行。

## 图片、文件、链接、语音和视频

原生 Feishu 适配器会下载图片和普通文件。链接作为文本交给 Agent，能否访问仍取决于本机网络和操作审批。语音必须额外配置语音转写，当前模板没有启用；视频当前只提供元数据和可能的缩略图，不会把视频正文交给 Agent。

图片和文件会暂存在工作区的 `.cc-connect/` 下；本项目的固定版本补丁会拒绝路径中的符号链接/非目录，把既有目录收紧为 `700`，并使用随机排他文件名以 `600` 写入，避免覆盖同名文件。目标工作区必须自行把 `.cc-connect/` 加入忽略规则；本项目仓库的 `.gitignore` 对外部工作区无效。仍然只使用小的非敏感样例验证：

1. 在执行线程中 `@` 本地机器人发送一张图片；
2. 再发送一个非敏感文本文件；
3. 确认 Agent 能看到内容，且群回复同时引用原消息并 `@` 发送者；
4. 确认日志没有记录文件内容、Token 或完整个人路径；
5. 不要把密钥、私密聊天记录或未经授权的附件作为测试数据。

## 费用

- Aily 直接处理轻任务：不消耗 Codex，但可能消耗 Aily 自身额度；
- Aily 生成内容后再转交：两边都可能产生模型用量；
- 直接给本地机器人发普通消息：使用 Codex；
- 查询状态、停止或拒绝任务：通常不需要新的模型调用；
- 飞书和 cc-connect 的软件费用、Codex 账户费用分别由对应账号承担。
