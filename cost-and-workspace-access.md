# 费用与工作区访问说明

## 费用链路

```text
飞书企业自建应用 -> 飞书连接器 -> cc-connect -> Codex -> 所选模型或服务商
```

| 环节 | 可能产生的消耗 | 通常由谁承担 |
|---|---|---|
| 飞书企业自建应用 | 消息事件与 OpenAPI 配额 | 飞书租户 |
| 上游机器人（若先生成再转交） | 该机器人的模型或套餐额度 | 上游机器人所有者或租户 |
| 飞书连接器（Feishu Connector） | 本机 CPU、内存和网络 | 运行机器的所有者 |
| cc-connect | 本机 CPU、内存和进程资源 | 运行机器的所有者 |
| Codex CLI | 所选账户的认证与模型用量 | Codex 账户所有者 |
| 模型或服务商 | Token、订阅额度或 API 用量 | 配置的服务商账户 |
| 可选文档、数据库或云工具 | 各自套餐和资源用量 | 对应服务账户 |

仅接收 WebSocket 事件、保持桥接服务（Bridge）连接或显示本地任务状态，不会产生模型 Token。只有消息被转发给 Codex 或其他模型服务时，才会产生对应模型用量。

若另一个智能体先总结任务，再把内容转交给本机器人，通常会形成两次独立模型调用；用户直接发给本机器人时通常只有后续 Codex 调用。飞书连接器无法判断或保证外部机器人的实际计费方案。

不要在文档中写死“本安装使用 API Key”或“本安装使用订阅额度”。安装时应确认实际登录方式，并以对应服务商当前的用量和账单页面为准。

## 为什么能力文件放在工作区内

Codex 只会加载当前项目或工作区中的指令和文件。启动飞书连接器的仓库文件位于 Agent 配置的 `work_dir` 之外时，Agent 拒绝读取是正常的安全边界，可避免它顺带访问运行时密钥。

因此，供默认 Agent 回答能力、费用和限制问题的脱敏说明放在：

```text
agent-workspace/CAPABILITIES.md
```

每个获准项目也可以拥有自己的 `AGENTS.md` 和能力说明。不要为图省事把整个仓库根目录设为默认工作目录，否则运行时配置、日志和会话数据会与 Agent 可读文件放在一起。

## 工作区模式

| 模式 | 典型用途 | 默认策略 |
|---|---|---|
| 默认工作区 | 帮助、规划和只读检查 | `suggest` 模式，窄目录 |
| 已批准只读项目 | 仓库分析和报告 | `suggest` 模式，禁止写入和 Git |
| 已批准开发项目 | 构建、测试和代码修改 | 应用服务（App Server），分支与工具审批 |
| 未知项目 | 权限申请与受控发现 | 批准前不派发 Codex |

获准路径必须经过规范化、确实是目录，并位于配置的安全根目录中。普通目录完全合法；仅当根目录存在有效 `.git` 时才按 Git 仓库处理。若发现嵌套仓库，应报告并等待确认，不得静默选取。

## 数据卫生

运行时配置、会话正文、消息链接、日志和任务状态可能包含个人信息与访问令牌。它们不得进入公开发行包；一旦暴露应立即轮换凭据，并在打包前运行脱敏检查。
