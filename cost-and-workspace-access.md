# 费用与工作区访问说明

## 费用链路

```text
Aily（可选） -> 飞书企业自建应用 -> cc-connect -> 所选本地 Agent -> 模型/服务商
```

| 环节 | 消耗 | 承担方 |
|---|---|---|
| 飞书企业自建应用 | 消息/OpenAPI 配额 | 飞书租户 |
| Aily | 套餐或模型额度 | Aily 所属账户/租户 |
| cc-connect | 本机 CPU、内存、网络 | 本机所有者 |
| 本地 Agent（Codex / Claude Code / Cursor） | 账号或 API 模型用量 | 对应 Agent 账户所有者 |
| MCP/云服务 | 各自 API 和资源费 | 对应服务账户 |

Aily 处理问候和飞书云端操作不会消耗本地 Agent；Aily 先总结再转交时，两边都可能产生模型用量。保持飞书 WebSocket 连接、执行停止命令或查看 Aily 自己维护的任务状态通常不产生新的本地 Agent 模型调用。

## 工作区

安装器要求一个初始工作目录，让第一次 Agent 会话有明确起点；它不是唯一工作区。主人之后可以用 cc-connect 原生 `/dir` 选择其他目录，目录可以是 Git 仓库，也可以是普通目录。消息里的 `workspace` 只是公开别名，不是绝对路径。

Aily 遇到其他工作区别名时先暂停，由主人确认本机路径和范围并执行 `/dir`。不要把整个主目录、安装目录或文件系统根作为工作目录，也不要让 Agent 为“找仓库”扫描未确认目录。

开发任务在派发前确认目标分支；确认分支不是对任何 Git 命令的批准。普通目录没有 Git 时继续按普通目录处理，不自动初始化仓库。

## 数据卫生

运行时配置、会话正文、消息链接、日志和附件可能包含身份信息或访问令牌，必须排除在发行包之外。发现泄露后先轮换凭据，再清理文件和历史。
