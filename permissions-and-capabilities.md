# 权限与能力说明

本文描述公开默认配置，不代表任何具体租户、用户、机器人、群或机器的当前状态。

## 默认权限矩阵

| 能力 | 默认状态 | 边界 |
|---|---|---|
| 飞书企业自建应用 | 本地配置后启用 | 租户应用、已发布版本和可用范围 |
| 私聊 | 仅白名单用户 | `allowedUserIds` |
| 群消息 | 必须明确 `@` | `allowGroupMessages`、群与发送者策略 |
| 来源机器人转交 | 配置前关闭 | `allowedBotIds` 与 `allowedBotChatIds` |
| 桥接服务（Bridge） | 仅本机 | 本机回环地址、随机令牌 |
| 管理接口（Management API） | 仅本机 | 本机回环地址、独立令牌 |
| 默认工作区 | 只读 | `agent-workspace`、Codex `suggest` |
| 已批准工作区读取 | 只读 | 静态项目（project）与规范化路径 |
| 已批准工作区开发 | 审批控制 | Codex 应用服务（App Server）；Git 根目录增加分支门禁 |
| 未知工作区 | 等待审批 | 安全搜索根目录与主人批准 |
| 图片、文件和语音 | 默认不转发 | 需要单独的媒体管线 |
| 生产数据库、Docker、Kubernetes | 默认未连接 | 启用前先增加窄化代理接口（Broker） |

## 当前可用

- 通过配置的 Codex 会话回答普通问题、能力、费用和帮助。
- 读取和分析默认工作区或明确批准工作区中的文件。
- 提取飞书文本、富文本和交互卡片中的文字与链接。
- 在飞书中报告任务编号、执行阶段、排队状态和耗时。
- 主人可以从飞书按编号停止任务。
- Git 根目录的开发任务先确认目标分支。
- 普通非 Git 目录不会被强制要求分支。
- 未知项目在搜索或登记前先申请工作区权限。

## 默认未授予

- 任意宿主机 Shell 或 root 权限；
- Docker Socket、生产 SQL、云管理员 API 或密钥仓库；
- 自动执行 `git switch`、`git commit`、`git push`、部署、删除或外发消息；
- 访问规范化获准工作区之外的文件；
- 读取全部群消息或私密飞书资源；
- 在无关的 Codex App 对话列表中查看这些独立任务。

实际工具审批行为取决于安装者选择的 cc-connect/Codex 后端。系统提示词不能替代操作系统隔离或确定性的策略网关。

## 扩展顺序

1. 确实需要图片或文件任务时，再增加媒体下载以及大小、类型限制。
2. 在工具注册表与审计日志之后增加只读 GitHub、飞书文档或数据库工具。
3. 为测试环境增加带预演（dry-run）与回滚的窄化部署代理接口（Broker）。
4. 生产访问必须使用独立凭据、新鲜审批，最好再加双人复核。
5. 只有真实任务需要跨小时或跨天暂停恢复时，才增加耐久工作流基础设施。
