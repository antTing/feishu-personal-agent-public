# 权限与能力说明

## 原生模式默认能力

| 能力 | 默认策略 |
|---|---|
| 飞书入口 | 企业自建应用 WebSocket 长连接 |
| 安装授权 | 官方 `PersonalAgent` 一键建应用；用户在浏览器批准，凭据和身份 ID 自动取得；缺权限自动发起管理员审批 |
| 群消息 | 只处理 `allow_chat` 中明确 `@` 的消息 |
| 来源 | 主人和明确的 Aily `open_id`，由同群一次性配对自动写入 |
| 会话 | 按飞书根消息线程隔离 |
| 并发 | Aily 对同一 `DS` 串行派发，不同 `DS` 线程可并行 |
| 媒体 | 图片和普通文件可下载；语音需另配转写；视频仅元数据/可能的缩略图 |
| 工作区 | 一个固定、明确授权的目录 |
| 本地 Agent | Codex、Claude Code 或 Cursor ACP；默认使用各自的保守权限模式 |
| 特权命令 | 只允许主人；高风险命令默认禁用 |
| 工具审批 | 固定版本补丁双层校验 `approval_from`，只允许主人且只允许单次审批 |
| 结果通知 | 群内回复引用原任务消息并 `@` 原发送者 |
| Bridge / Management | 原生模式不启用 |

## 由 Aily 负责

- 判断是否需要本机；
- 生成 `DS/T` 并维护来源会话映射；
- 同一任务继续回复同一执行线程，新任务创建新线程；
- 把最终结果转回原始群/私聊；
- 无法判断续问或新任务时询问用户。

## 尚未硬化的能力

- 从任意线程按 `T-...` 精确停止；
- 未知工作区自动申请和受控发现；
- 只读/开发工作区动态切换；
- Git 分支的确定性门禁；

这些能力在旧 Connector 中有部分实现，但不会原样搬回。原生 PoC 验证缺口后，只在 cc-connect 上增加窄补丁。

## 默认不授予

- root、任意宿主机 Shell、Docker Socket；
- 生产数据库写权限、云管理员凭据、SSH 私钥；
- 自动 Git、部署、删除、外发或生产变更；
- 读取工作区外文件、全部群历史或私密飞书资源。
