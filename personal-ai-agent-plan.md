# 个人 AI Agent V2 最小方案

## 架构

```text
用户 -> Aily（轻任务和分流）
          -> 执行群 @ 本地机器人
          -> cc-connect 原生 Feishu
          -> Codex
          -> 主人当前选择的本地目录
```

不再重复维护自定义飞书长连接、媒体解析和 Bridge。旧 Connector 只在迁移期间保留。

## 分工

- Aily：判断是否需要本机，维护 `DS/T`、来源会话和执行线程映射；
- 飞书执行群：主人、Aily、本地机器人共享状态和结果的控制面；
- cc-connect：长连接、图片/普通文件、线程会话、进度、停止和 Codex 生命周期；
- Codex：在明确工作区内推理和执行；
- 主人：浏览器/飞书授权、工作区、分支、Git 和工具审批；安装器自动取得并保存秘密和身份值。

## 会话模型

同一任务链使用同一飞书回复线程，由 Aily 持久化 FIFO 并一次只派发一个 `T`；不同任务链创建不同根消息线程并行。群 ID、Aily 身份或用户身份都不能单独定义任务链。

## 实施阶段

1. 原生初始目录 PoC：文本、图片、普通文件、进度、`/stop` 和主人 `/dir`；
2. 验证同群不同根线程及跨群会话隔离；
3. 验证最终答复能触发并 `@` 来源 Aily；
4. 验证工具审批的操作者身份；
5. 只对确认缺失的能力增加窄补丁：精确任务控制、主人审批、来源机器人提醒；
6. PoC 通过后移除旧 Connector、Bridge 和 Management 依赖；
7. 工作区别名、只读/开发和分支门禁留在 Aily 策略层；目录切换复用主人专属的原生 `/dir`，不另造注册系统。

## 验收

- 非白名单用户、机器人和群不能触发任务；
- 图片和附件可用；
- 同一 `DS` 由 Aily 串行派发、不同线程并行；
- 主人和 Aily 能在正确线程停止任务；
- 只有主人批准工具；
- 最终结果回到来源 Aily；
- 普通目录不强制 Git；
- 日志和回复不泄露身份、密钥或完整路径。
