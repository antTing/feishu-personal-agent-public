# 个人 AI Agent 最小落地方案

## 决策

第一阶段只保留一条简单链路：

```text
飞书企业自建应用
  -> 飞书连接器（Feishu Connector）
  -> cc-connect 桥接服务（Bridge）
  -> Codex
```

在真实使用证明有必要前，不增加多模型 Router、LangGraph、n8n、Dify 或 OpenHands。对于个人系统，结构越小，越容易保护权限、定位问题和维护发布。

## 职责

- **飞书企业自建应用**：接收消息和发送回复。
- **飞书连接器（Connector）**：白名单、消息解析、任务编号、状态、停止、工作区审批和回复上下文。
- **cc-connect**：长驻 Agent 会话与命令行工具生命周期。
- **Codex**：推理、代码检查和经过审批的执行。
- **可选工具**：每次只增加一个窄权限 MCP 或 API 集成，先只读再考虑写入。

## 首个版本范围

### 日常工作

- 总结消息和文档；
- 生成方案、报告和检查清单；
- 检查明确配置的本地工作区。

### 开发

- 分析代码仓库；
- 根目录是 Git 仓库时先确认目标分支；
- 提出修改，并在审批后编辑文件和运行测试；
- 根目录没有 Git 元数据时按普通目录工作。

### 运维

- 在明确批准的目录内做只读健康检查和日志分析；
- 提出命令与部署方案；
- 写入、重启、删除和生产变更必须单独确认。

## 安全不变量

1. 飞书连接器不接受消息扩大自身白名单或工作目录。
2. 所有工作区路径都要规范化，并限制在配置的安全根目录中。
3. 桥接服务（Bridge）和管理接口仅监听本机回环地址（loopback）。
4. 运行时密钥与会话正文不得进入公开源码树。
5. 确认分支不等于批准 Git 命令。
6. 提示词不是沙箱；敏感系统应使用独立操作系统用户、容器和窄化代理接口（Broker）。

## 交付阶段

1. 使用虚构测试消息打通飞书链路。
2. 增加一个已批准只读工作区，验证状态、停止和重连。
3. 增加一个使用应用服务（App Server）审批的开发工作区。
4. 使用最小 `include_bot` 权限增加机器人转交。
5. 增加带工具白名单和审计日志的只读外部工具。
6. 仅在有 dry-run、差异确认和回滚方案时增加测试环境部署。
7. 只有任务确实跨越数小时或数天时，才考虑耐久工作流编排。

## 验收标准

- 未授权用户、机器人和群不能触发任务；
- 重复事件不会重复执行；
- 每个活动任务都有编号，主人可按编号停止；
- Git 与非 Git 工作区符合文档行为；
- 回复和日志不包含秘密、具体私有 ID 或完整本地路径；
- 新机器可以只参考 `INSTALL.md` 完成安装，不需要复制任何私有运行时文件。
