# 安全策略

## 支持范围

安全修复只针对当前主分支和文档明确固定的 cc-connect 版本。运行私有 fork、关闭补丁校验、开放公网端口或启用高权限工具后，风险由部署者负责评估。

## 报告漏洞

请使用代码托管平台的私密 Security Advisory 或维护者公布的私密安全渠道。不要在公开 Issue 中粘贴 App Secret、Token、用户/机器人/群 ID、飞书消息链接、会话文件、日志、二维码或本地路径。

报告应包含可复现步骤、受影响版本和经过脱敏的影响说明。真实凭据应先轮换，再讨论细节。

## 密钥与个人数据

- 所有真实配置和状态只能位于 `runtime/`，不得提交或打包。
- 配置文件使用 `600`，目录使用 `700`。
- Bridge Token、Management Token 和飞书 App Secret 相互独立。
- 日志不得记录消息正文、ID、Token、Cookie、完整路径或工具输入中的秘密。
- 二维码、截图、会话 JSON 和飞书资源链接都可能包含个人数据或访问令牌。
- 一旦凭据出现在聊天、日志、截图、提交或发行包中，应立即轮换；删除文件不等于撤销凭据。

本地 Token 和飞书 App Secret 的安全轮换步骤见 [OPERATIONS.zh-CN.md](OPERATIONS.zh-CN.md)。轮换前必须停止服务；App Secret 由主人在独立终端输入，Agent 不得代填或读取。

## 网络边界

本项目的 cc-connect 补丁把 Bridge 与 Management API 固定到 `127.0.0.1`。不要通过端口转发、反向代理或容器端口映射把它们暴露到局域网或公网。飞书入口使用官方 WebSocket 长连接，只需要出站网络。

Management API 能重启 Agent 运行时，因此即使只在 loopback 也必须使用强随机 Token。不要把 Token 放入 URL、命令行历史或日志。

## 身份与权限

- `allowedUserIds` 至少有一个明确主人，禁止通配符。
- 机器人转交同时限制来源机器人和群。
- 群聊只处理明确 `@` 当前机器人的消息。
- 工作区路径必须是登记路径或受控根目录下的真实目录，禁止把主目录、文件系统根或秘密目录作为搜索根。
- 不要向 Agent 挂载 Docker Socket、SSH Agent、云管理员凭据或生产数据库写账号。
- 写入、命令、Git、部署、删除、外发和生产操作应逐次审批。

提示词、`AGENTS.md` 和工具说明不是安全边界。真正的隔离需要独立操作系统用户、容器/VM、最小文件权限、网络出口限制和窄化的操作 Broker。

## 不可信输入

飞书消息、富文本、卡片、链接、仓库文件和工作区指令都可能包含提示注入。它们不能修改白名单、扩大工作区、读取密钥或绕过审批。访问网页、MCP、数据库和云服务时应使用最小权限凭据与独立审计。

## 发布检查

发布前必须运行：

```bash
npm test --prefix feishu-connector
./scripts/release-check.sh
```

该检查是启发式防线，不能证明不存在秘密。正式发布应始终使用 `./scripts/export-public.sh <源码树之外的新目录>` 生成白名单导出包，再对导出目录运行托管平台的 Secret Scanner 并人工抽查；不支持直接压缩或上传当前工作目录。

还要人工确认发行包不含 `runtime/`、`managed-workspaces/`、`.claude/`、`node_modules/`、`.env*`、日志、运行时锁文件、图片、二维码、会话、缓存和本机二进制。`package-lock.json` 是可复现安装所需的公开依赖锁定文件，应保留并审查。

若敏感文件曾被版本控制系统记录，仅添加 `.gitignore` 不会清除历史。应在明确审批后审计已跟踪文件和完整历史，轮换全部相关凭据，并按托管平台流程清理历史。
