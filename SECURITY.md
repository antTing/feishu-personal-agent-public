# 安全策略

## 私有数据

- 真实 App ID、App Secret、用户/机器人/群 ID、会话、日志和附件只允许位于被忽略的 `runtime/`；
- 私有目录使用 `700`，配置文件使用 `600`；
- 不在聊天、Issue、截图、日志或发行包中回显密钥、身份 ID、消息链接和完整个人路径；
- 凭据一旦出现在上述位置，立即轮换。删除文件不能撤销已经泄露的凭据。

原生模式只需要飞书 App 凭据，不启用 Bridge 或 Management。旧 Connector 兼容模式仍依赖两个本机端点，它们必须保持 loopback，不能通过代理或端口映射暴露到局域网或公网。

## 身份和来源

- `allow_from` 只填写主人和明确的上游调度机器人，禁止 `*`；
- `allow_chat` 只填写专用执行群；
- `group_reply_all = false`，群聊必须明确 `@` 本地机器人；
- `thread_isolation = true`，一个根消息线程对应一个 Agent 会话；
- Aily 可以提交任务和在对应线程请求停止，但不能被当作工具审批人；
- 本项目固定版本补丁在核心层校验项目 `approval_from`，并在飞书卡片层再次校验平台 `approval_from`；两处都只配置主人；
- 配置 `approval_from` 后禁用“允许全部”，每次工具调用都必须单独处理；
- 执行群向白名单来源保留 `/stop`、`/help`、`/version`；`/dir` 和 `/workspace` 只允许主人，且文字命令和卡片按钮使用同一门禁；`/cancel`、`/status`、`/usage`、会话、模型、Provider、历史、调度器和管理类命令全部禁用，避免白名单调度机器人或提示注入扩大权限、泄露路径/账号或重置会话；
- 不把多个独立 `T` 同时派入同一个 cc-connect 线程；Aily 负责持久化队列，避免 `/stop` 清理线程等待消息时误伤其他任务；
- 卡片的其他操作也必须先通过 `allow_from`，未授权群成员不能借卡片回调操作会话。

## 工作区

- 安装参数只设置初始工作目录，不构成永久的单工作区白名单；
- 只有主人可以通过 cc-connect 原生 `/dir` 或主动启用后的 `/workspace` 切换目录；Aily、普通消息和任务封装中的路径不能触发切换；
- 主人切换前自行确认路径、访问范围和是否创建/克隆；切换目录不是对 Git、部署或其他工具操作的批准；
- 工作区可以没有 Git；只有确认存在 Git 仓库时才询问分支；
- 任何 Git、部署、删除、外发、写数据库和生产操作仍需明确审批。

提示词和 `AGENTS.md` 不是操作系统安全边界。需要强隔离时使用独立 OS 用户、容器/VM、最小文件权限和受控网络出口。不要向 Agent 提供 Docker Socket、SSH Agent、云管理员凭据或生产数据库写账号。

## 不可信输入和媒体

飞书消息、图片、文件、语音、视频元数据、链接和仓库内容都可能包含提示注入。媒体支持不代表可以转发秘密；限制附件大小和类型，不自动执行附件中的脚本，不让消息扩大白名单或工作区。图片和文件暂存在工作区 `.cc-connect/` 下；固定版本补丁拒绝其中的符号链接/非目录，收紧既有目录为 `700`，并用随机排他文件名以 `600` 写入，避免同名覆盖。部署的工作区也必须忽略该目录。跨平台实现仍无法防御同一 OS 用户在检查与创建之间主动替换目录的理论竞态，因此不要让其他不可信进程共享该服务账号。

## Agent 代装边界

Agent 可以检查依赖、构建、测试并启动 `onboard-native.sh`。自动安装器通过飞书官方授权页取得 App 凭据和主人身份，再通过一次性群消息取得执行群并配对 Aily；Agent 不得要求用户在聊天中提供这些值，也不得读取安装器生成的 `.onboarding.json` 或最终配置正文。浏览器授权、企业管理员审批、把机器人加入群和发送配对消息都由主人在飞书界面确认。

配对期间不启动 cc-connect/Codex。安装器使用 128 位随机码，只接受明确 `@` 新机器人、包含完整当前随机码的文本、富文本或卡片消息；执行群配对消息还必须来自扫码账号。Aily 必须在同一执行群使用另一枚随机码报到，随后主人必须核对发送者，并回复该条 Aily 消息、`@` 新机器人、使用一次性确认码或拒绝码。确认后候选机器人才进入白名单；拒绝后候选身份会从私有状态移除，并生成新的调度配对码，旧码不能继续使用。配对完成后才生成配置并删除临时状态和本机配对页。默认只在本机浏览器显示短时授权链接和配对码；`--no-open` 必须与 `--show-pairing-codes` 同用且只限本人独立终端。遗留锁只允许主人确认没有安装器运行后用 `--recover-lock` 恢复；代装 Agent 不得使用这些选项。

所有 Git 命令、系统包安装、配置覆盖、删除、开机自启动和网络暴露都需主人明确批准。

## 发布检查

```bash
npm test --prefix feishu-connector
./scripts/release-check.sh
```

正式发布使用 `./scripts/export-public.sh <源码树之外的新目录>`。人工确认不包含 `runtime/`、`managed-workspaces/`、`.claude/`、`node_modules/`、环境文件、日志、会话、截图、二维码、附件、缓存和本机二进制。扫描是启发式防线，不能替代人工复核和托管平台 Secret Scanner。
